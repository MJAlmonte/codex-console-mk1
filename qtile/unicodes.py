from libqtile.widget.textbox import TextBox

def slant_divider(bg_color, fg_color):
	return TextBox(
		widget.TextBox(
			text="Gello",
			padding=0,
			fontsize=20,
			background=bg_color,
			foreground=fg_color,)
